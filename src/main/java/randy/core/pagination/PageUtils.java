package randy.core.pagination;

import java.util.Collection;

/**
 * 페이징처리 관련 유틸 클래스.
 * 
 * @author jace
 */
public class PageUtils {

	private PageUtils() {}
	
	/**
	 * 페이징이 처리된 정보를 반환한다.
	 * 
	 * @param items
	 * @param totalRow
	 * @param startRow
	 * @return PageItems<T>
	 */
	public static <T> PageItems<T> getPageItems(final Collection<T> items, int totalRow, int startRow) {
		
	}
	
	/**
	 * 페이징이 처리된 정보를 반환한다.
	 * 
	 * @param items
	 * @param totalRow
	 * @param startRow
	 * @return PageItems<T>
	 */
	public static <T> PageItems<T> getPageItems(final Collection<T> items, int totalRow, int startRow) {
		
	}	
	
}
